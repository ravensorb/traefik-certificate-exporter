## What does this change?

<!-- A clear description of what this PR does and why. -->

## Checklist

- [ ] `poetry run pytest` passes
- [ ] `pre-commit run --files <changed files>` is clean (not `--all-files`, see
      [CONTRIBUTING.md](../CONTRIBUTING.md))
- [ ] If a CLI flag / config key / env var changed: README.md, docker/README.md,
      config.sample.yml, and config_default.yaml are all updated consistently
- [ ] If this fixes a reported issue, it's referenced (`Closes #...`)
