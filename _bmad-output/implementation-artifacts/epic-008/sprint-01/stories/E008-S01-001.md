---
key: 'E008-S01-001'
title: 'Publish Immutable Development Packages and Images'
status: done
classification: complex
---

> **Superseded in part (E008-S01-003).** This story describes
> `scripts/forge_coordinates.py`, `scripts/stable_tags.py` and/or `scripts/finalizer_gate.py`
> as new surface. All three were deleted later in the same sprint: the maintainer's rule for
> this pipeline is actions and workflow steps rather than scripts, and each decision now lives
> in the workflow step that owns it, executed by the contract suite with the real environment
> substituted in. See ADR-0010, ADR-0011 and
> `test_no_publication_decision_lives_in_a_python_module`. Read the module references below as
> history, not as current design.


# Publish Immutable Development Packages and Images

As a maintainer, I want protected default-branch artifacts published from the verified bundle,
so development delivery creates immutable package and image identities without rebuilding.

Depends on the completed Epic 7 foundation.

## Acceptance Criteria

**Given** a protected default-branch push, **when** `dev.yaml` starts, **then** unknown forges fail
closed; one full-history/tags checkout derives `X.Y.(Z+1).devN`; an exact stable tag on the source
suppresses dev publication; and `verify-build.yaml` is called exactly once for the event SHA.

**Given** a destination receives `verified-dist-v1`, **when** it reaches its first credentialed
step, **then** it has revalidated `SHA256SUMS` and `build-manifest.json`, matched source/version,
and, for images, matched the exact wheel hash to the declared image input (CI-AR36).

**Given** package publication is enabled or required, **when** upload begins, **then**
`pypa/gh-action-pypi-publish@<reviewed-full-commit-sha>` receives only the verified wheel/sdist;
implementation resolves, reviews, and records the actual full SHA. Package OIDC/tokens are
isolated from image jobs (CI-AR24, CI-AR38, CI-AR40).

**Given** development image publication, **when** the image job runs, **then** it consumes the
exact verified wheel and performs one Buildx invocation for `linux/amd64,linux/arm64` with the
immutable `dev-<12sha>` tags for the required forge registry and optional Docker Hub. It never
rebuilds per registry and emits digest/platform inspection as outputs and run summary (CI-AR39,
CI-AR41).

**Given** forge coordinates, **when** `FORGE_REGISTRY` is present, **then** it accepts only
same-forge `host[:port]` with no userinfo, path, query, or fragment. Optional toggles accept only
`true`, `false`, or absence; disabled destinations receive no credentials or requests.

## Multi-platform feasibility — spike complete, do not re-run it

Gate finding F13 required a spike before this story proceeds. It ran on 2026-09-02 and the
answer is **feasible as-is, with no Dockerfile change**. Evidence: `/tmp/f13-spike/` (transient);
numbers and reasoning recorded permanently in ADR-0008, whose second open question this closes.

- `linux/amd64` alone: **50 s**. `linux/amd64,linux/arm64` emulated: **436 s** (8.7×).
- 80% of the wall is two `linux/arm64` steps under QEMU: `apk add gcc cargo` (203 s) and
  `poetry install --only main` (145 s). Nothing compiles Rust from source.
- Budget **12–15 minutes** on a 4 vCPU GitHub-hosted runner (the spike had 8). No second native
  runner is needed on these numbers.
- The wheel is `py3-none-any`, so the same file with the same SHA-256 installs into both platform
  images. Exact-wheel provenance is platform-independent by construction.

### The index assertion this story must write

`ATTESTATIONS = []` — the shipped default — **does not disable attestations**. BuildKit emits
SLSA provenance `mode=min` regardless, so the index carries four descriptors: two platforms plus
two `unknown/unknown` attestation manifests. Adding `type=sbom` keeps it at four; only
`type=provenance,disabled=true` drops it to two. **A count assertion is wrong in three
directions.** Key on the authoritative annotation, not on the `unknown/unknown` convention:

```python
platforms = {
    f"{m['platform']['os']}/{m['platform']['architecture']}"
    for m in index["manifests"]
    if m.get("annotations", {}).get("vnd.docker.reference.type")
    != "attestation-manifest"
}
assert platforms == {"linux/amd64", "linux/arm64"}
```

Nesting differs by source: `imagetools inspect --raw <ref>` returns the manifest list directly,
while an OCI-layout `index.json` sits one level above it, and a single-platform layout has no
nested index at all. Iterating `index.json`'s `manifests[]` reads the wrong level.

### Two bake-interface traps found by the spike

Neither blocks, both cost an hour if rediscovered:

1. Buildx parses env overrides of `list(string)` as CSV, so `OUTPUTS="type=oci,dest=/tmp/x.tar"`
   splits into two elements. It must be CSV-quoted: `OUTPUTS='"type=oci,dest=/tmp/x.tar"'`.
   `PLATFORMS="linux/amd64,linux/arm64"` works naturally because there each comma *is* a
   boundary. The failure is opaque — `ATTESTATIONS="type=provenance,mode=max"` reports
   `Unsuitable value: attestation type not specified`, which does not read as a quoting problem.
2. `type=oci,dest=` writing outside the bake context needs `--allow fs.write=<dir>`, otherwise
   `ERROR: additional privileges requested`. Local and spike use only; irrelevant to
   `type=registry`.

Gate finding F25 (`DOCKER_PLATFORMS` baked into the runtime image's `ENV`) is **already fixed** —
it was read nowhere and has been deleted.

## Destination matrix — channel decides, and channel is the file you are in

`dev.yaml` is always the development channel and `release.yaml` always the stable one, so this
distinction costs no runtime condition (ADR-0011).

| Destination | `dev.yaml` | `release.yaml` |
|---|---|---|
| image -> active forge registry | **always** | **always** |
| image -> Docker Hub | **never** | if `PUBLISH_IMAGE_DOCKERHUB` |
| package -> forge Python index | if supported *(host capability)* | if supported *(host capability)* |
| package -> TestPyPI | if `PUBLISH_PACKAGE_TESTPYPI` | **never** |
| package -> PyPI | **never** | if `PUBLISH_PACKAGE_PYPI` |

No toggle is renamed or removed; each is channel-scoped, so `PUBLISH_IMAGE_DOCKERHUB` is inert in
`dev.yaml`. Development images never reach Docker Hub.

Two reasons a destination may be absent, recorded distinctly because conflating them hides a
regression: **disabled by toggle** (an operator chose it) and **absent by host capability**
(GitHub has no forge Python index, and no toggle can override that).

The enabled set is produced **once**, by the plan job, as a job output -- not a repository-owned
schema (CI-AR41), and reviving neither retired CI-AR22 nor CI-AR23. No publisher re-reads
`PUBLISH_*`; a guard enforces it.

## Shared surface — name it now, before two workflows restate it (F8)

`dev.yaml` and `release.yaml` would otherwise hold near-identical implementations of forge
detection, bundle revalidation, and the image build — then stories 003 and E009-S01-001 would
edit both. Four stories touching two files that should share one implementation is how the
divergence starts. This story owns the shared surface:

- `.github/actions/forge-coordinates/action.yml` — derives forge kind, registry and package-index
  coordinates from action context, fail-closed, with `FORGE_REGISTRY` as the documented override
  (CI-AR6). Also emits whether a forge Python index exists on this host, which is the
  capability half of the enabled set (see the destination matrix above).
- `.github/actions/verified-bundle/action.yml` — downloads `verified-dist-v1`, checks
  `SHA256SUMS`, validates `build-manifest.json`, and matches source SHA, version and the exact
  wheel hash before any login (CI-AR36).
- `.github/workflows/publish-image.yaml` — reusable, taking channel, tags and enabled registries;
  one Buildx multi-platform invocation, no per-registry rebuild (CI-AR39).

`release.yaml` consumes these rather than restating them.

## Validators are Python with tests, not regexes in `run:` blocks (F9)

Two rules need real parsing and are not unit-testable inside a `run:` block:

- **Forge coordinate validation** — rejecting userinfo, path, query and fragment in a
  `host[:port]` override is URL parsing. Use `urllib.parse`, not a hand-written pattern.
- **Annotated-tag peeling and reachability** — `git rev-parse` / `merge-base --is-ancestor`
  wrapped, not pattern-matched.

Both live in `scripts/` (or `src/publication_contract/`) as functions with pytest cases, and the
workflow calls them. Left inline, the only available "test" is grepping the workflow for its own
regex, which asserts nothing about behaviour. This is the same rule that put the release
transaction in `scripts/release_version.py` rather than in a `run:` block.

## The published image must be smoke-tested, not only inspected (F14)

`verify-build.yaml` builds a **single-platform, `--load`ed** image and smoke-tests it. The
publisher then builds a **different** image — multi-platform, pushed — which nothing ever starts.
That is ADR-0008's Option-B objection reappearing one layer up: the artifact that was tested and
the artifact that ships are two different builds. Digest and platform *inspection* is not a smoke
test; it proves the manifest exists, not that the container runs.

After push and before any alias moves: pull the published index by digest, resolve the descriptor
matching the runner's own architecture, and re-run the container smoke test
(`LiquidLogicLabs/git-action-docker-test@v2`) against it.

**Stated limit, because it should not be discovered later:** only the native descriptor is
executed. The non-native platform is covered by the argument ADR-0008 now records — the wheel is
`py3-none-any`, so the *same file with the same SHA-256* is installed into both images, and the
base is digest-pinned — plus platform inspection. That is provenance, not execution, and the
runbook should say so.

## The tag race is accepted, and that acceptance is a record not an oversight (F16)

`just release` pushes the branch and the tag **atomically**, so one operation fires a `push`
event on `main` *and* a tag event. Dev suppression depends on `dev.yaml` observing the exact
stable tag on the source commit — and the forge guarantees no ordering between the two.

If the tag is not yet visible when the dev run's guard job evaluates, an **immutable,
unretractable** `X.Y.(Z+1).dev0` is published for the release commit. It cannot be withdrawn;
the recovery is a new development version.

**This was knowingly accepted** — tagging drives the release, and serialising the two events
would mean giving up the atomic two-ref push that ADR-0006 exists to provide. The superseded
plan recorded that acceptance; the rewrite dropped the record, so the next reader met it as a
bug rather than a decision. It is recorded here again for that reason.

**Cheap hardening, which does not remove the race:** re-run `git fetch --tags` and
`git describe --exact-match` immediately before the **first credentialed dev step**, not only in
the guard job. That shrinks the window from "guard job start → upload" to "check → upload"
without pretending it is closed.

**Test anchor:** plant a tag arriving between the guard job and the upload; suppression must
still fire.

## Files in scope

- `.github/workflows/dev.yaml` (new)
- `.github/actions/forge-coordinates/action.yml` (new — F8 shared surface)
- `.github/actions/verified-bundle/action.yml` (new — F8 shared surface)
- `.github/workflows/publish-image.yaml` (new, reusable — F8 shared surface)
- `scripts/` — forge-coordinate and annotated-tag validators as importable functions (F9)
- `tests/` — pytest cases for those validators (F9); they are the reason the validators are
  not inline
- `scripts/committed_versions.py` — the dev version comes from `development_version(distance)`
  and nowhere else (F12)
- `.github/actions/setup-poetry-python/action.yml` — note its input set is asserted by **exact
  equality**, so adding a `development-distance` input breaks a shipped test until that
  assertion is updated in the same change (F12)
- `docker/Dockerfile` — the `ARG BASE_IMAGE` default is already restored; do not reintroduce a
  bare `ARG BASE_IMAGE`, which silently disables the dependabot docker entry
- `tests/ci/test_workflow_contracts.py`
- `tests/ci/fixtures/workflows/` (dev push event fixture)

## Definition of Done

- `dev.yaml` owns protected default-branch pushes and uses workflow concurrency to cancel/supersede
  stale candidates.
- Required forge and enabled external immutable publications consume one verified artifact set.
- The image job receives only enabled registry credentials; package credentials remain job-local.
- Tests cover forge fail-closed, full-history derivation, stable-tag suppression, coordinate
  validation, one verifier call, one Buildx invocation, two platforms, and secret isolation.
- Run evidence records run ID/URL, manifest/artifact hashes, published digest/platforms, and
  destination conclusions without adding a schema (CI-AR37, CI-AR41).

## Test anchors

Guard, then the violation planted to prove it fires:

- Forge fail-closed: unknown forge hits a non-zero fallthrough; plant deleting that branch.
- Full-history derivation: parsed checkout keeps full depth and tags; plant deleting the key while
  leaving the comment explaining it.
- Stable-tag suppression: every publisher is transitively gated on the suppression job; plant one
  that is not (scope attack).
- Coordinate validation: `FORGE_REGISTRY` rejects userinfo, path, query, fragment, foreign host;
  plant an unanchored pattern.
- One verifier call: parsed verifier `uses:` in `dev.yaml` counts one; plant a second elsewhere.
- One Buildx invocation: one build-push step carries both platforms and all tags; plant a
  per-registry second build.
- Secret isolation: per-job secret references stay disjoint; plant a registry credential in the
  package job.

## Out of Scope

- Moving `dev` or any stable alias; Story 8.3 owns every alias finalizer.
- Stable exact-tag publication (Story 8.2) and Gitea certification (Epic 9).

## Prep finding P1 — the post-push inspection cannot share a file with the alias finalizer

Found at story prep against the shipped guards; it is a placement constraint, not a change of
intent, and it is written here so it is met by design rather than discovered as a red test.

`test_alias_ordering_is_decided_from_git_never_from_a_registry` forbids
`docker manifest inspect`, `buildx imagetools inspect`, `skopeo inspect|list-tags` and
`crane ls|digest|manifest` in **every job of any workflow file that moves an alias** — the scope
is deliberately the whole file, so a registry read in the plan job cannot decide ordering and
hand the answer downstream one job earlier.

Story 003 puts an alias finalizer into `dev.yaml` and `release.yaml`. From that moment those two
files are alias-moving, and the `buildx imagetools inspect --raw` this story mandates for the
index/platform assertion, and for resolving the native descriptor before the published-image
smoke test, would fail that guard **inside the same file**.

Note this is *not* the sibling rule: `test_no_publisher_queries_a_destination_before_uploading`
already carves out post-push inspection by position relative to the upload. This guard has no
such carve-out and needs none, because the placement below satisfies both.

**Resolution, requiring no guard change:** every read of a published image — the raw index fetch,
the filtered-platform assertion, the digest resolution and the pulled-by-digest smoke test —
lives in `.github/workflows/publish-image.yaml`, which this story already owns and which moves no
alias. `dev.yaml` and `release.yaml` consume its outputs (`digest`, `platforms`) and never run an
inspect of their own. Do **not** resolve this by widening, renaming around, or deleting the
guard; the regexes above are the rule, and evading them with an equivalent registry read in
Python is the same violation spelled differently.

**Test anchor:** plant a `buildx imagetools inspect` in a `dev.yaml`/`release.yaml` job once a
finalizer exists there — it must fail.

## Prep finding P2 — this story makes the workflow set five files, not four

`.github/workflows/publish-image.yaml` is a fifth workflow file on disk. Story 004's exact-topology
AC currently names four (`ci.yaml`, `dev.yaml`, `release.yaml`, `verify-build.yaml`) and its test
anchor plants "a fifth workflow file" as a violation. That anchor would reject this story's own
shared surface. The reconciliation is recorded in story 004: the expected set is those four plus
`publish-image.yaml`, distinguished as reusable-only (`workflow_call`, no event triggers of its
own), exactly as `verify-build.yaml` already is.

## Technical AC dimensions (step-02 gate record)

| Dimension | Where |
|---|---|
| Interface contracts | shared-surface action/reusable-workflow list (F8); enabled set as a plan-job output; `digest`/`platforms` outputs |
| Error and edge cases | forge fail-closed; coordinate rejection set; stable-tag suppression and the accepted tag race (F16) |
| Observability | digest/platform inspection as outputs and run summary; run ID/URL, manifest and artifact hashes, destination conclusions (CI-AR37, CI-AR41) |
| Security | per-job secret isolation; package OIDC/token job-local; no credentials to disabled destinations; SHA-pinned PyPI action (ADR-0009); bundle revalidation before any login (CI-AR36) |
| Testability | validators are importable Python with pytest cases, not regexes in `run:` blocks (F9); every test anchor names the planted violation |
| Existing-library check | `pypa/gh-action-pypi-publish` (SHA-pinned), Buildx bake for the multi-platform build, `LiquidLogicLabs/git-action-docker-test@v2` for the smoke test, `urllib.parse` rather than a hand-written host pattern, `git rev-parse`/`merge-base` rather than tag pattern-matching |

## Implementation record

### What landed

| File | Role |
|---|---|
| `.github/workflows/dev.yaml` | development channel; `push` on `main`, one verifier call, three publishers, evidence job |
| `.github/workflows/publish-image.yaml` | reusable, one Buildx multi-platform invocation; owns every published-image read (P1) |
| `.github/actions/forge-coordinates/action.yml` | CI-AR6 coordinates, delegating to the tested validator |
| `.github/actions/verified-bundle/action.yml` | CI-AR36 boundary revalidation, shared by both channels (F8) |
| `scripts/forge_coordinates.py` + `tests/test_forge_coordinates.py` | `urllib.parse`-based override validation (F9) |
| `scripts/stable_tags.py` + `tests/test_stable_tags.py` | annotated-tag peeling and reachability, delegated to git (F9) |
| `.github/actions/setup-poetry-python/action.yml` | gained `development-distance` / `development-version` (F12) |
| `tests/ci/test_workflow_contracts.py` | 11 new guards, each proven by a planted violation |
| `tests/ci/fixtures/workflows/push-main.json` | dev push event fixture |
| `docs/operational.md` | development-publication runbook, including the two stated limits |

### Decisions taken during implementation

- **The reviewed PyPI publisher SHA is `dc37677b2e1c63e2034f94d8a5b11f265b73ba33`** — the head of
  `pypa/gh-action-pypi-publish`'s `release/v1`, which carries tag `v1.14.2`, committed 2026-07-28.
  Resolved from the upstream repository and reviewed 2026-09-02; both facts are recorded beside
  each `uses:` line, and a new guard
  (`test_every_sha_pinned_publisher_records_the_date_it_was_reviewed`) fails if that record is
  removed. The shipped guard enforces only the *shape*, which a copied-from-anywhere SHA also
  satisfies.
- **`development-distance` was added to `setup-poetry-python`** rather than having the plan job
  call `committed_versions.py` itself, so no plan job has to know how a development version is
  spelled. The exact-equality input assertion was updated in the same change, deliberately (F12).
- **Suppression is decided after the verifier, not before it.** `stable-tag-guard` `needs: [plan,
  verify]`, so the tag set is read as late as possible before the first credentialed job starts;
  the two step-based package publishers additionally re-read it immediately before their upload.
  The cost is that a release commit still runs a full verification before publication is
  suppressed — which is wanted, since verifying `main` is what BL-E008-001 asks for. The race is
  narrowed, not closed, exactly as F16 records.
- **The image publisher cannot re-check the tag set inline**, because `dev.yaml` reaches it through
  `uses:` and a called workflow takes no caller steps. It is protected at job granularity by
  `needs:` plus its `if:`. Pushing the channel-specific suppression rule down into the shared
  reusable workflow was rejected: it would put development-channel logic in a file `release.yaml`
  also consumes.
- **Reachability is used, not merely available.** `stable-tags.py --reachable-from` is wired into
  the guard job, which refuses to publish when the pushed commit has been force-pushed off the
  protected default branch.
- **Secret isolation is structural**: the forge index job holds `FORGE_PACKAGE_TOKEN` only,
  TestPyPI holds an OIDC identity and no secret at all, and the image job holds `GITHUB_TOKEN`
  only. A new guard asserts pairwise disjointness across publishers derived from capability.

### Guard reach, and the one refactor to a shipped guard

`REGISTRY_READ_COMMANDS` was hoisted out of
`test_alias_ordering_is_decided_from_git_never_from_a_registry` to module scope so the new
placement guard consumes the same patterns. The patterns themselves are unchanged; two hand-kept
copies would drift and the copy nobody updated would be the one still reporting success. No guard
was weakened, narrowed or deleted.

`test_channel_workflows_consume_image_inspection_rather_than_performing_it` enforces P1 **now**,
rather than leaving story 003 to discover it as a red test: any workflow that calls
`publish-image.yaml` may not read a registry itself. Its scope is derived from who calls the image
publisher, so `release.yaml` is covered the day it lands.

### Verification

- `poetry run pytest -q` — 198 passed (150 before this story: +11 workflow guards, +37 validator
  cases).
- `pre-commit run actionlint` over both new workflows and all three actions — passed.
- `pre-commit run ruff-check` / `ruff-format` / `mypy` / `check-yaml` / `mixed-line-ending` /
  `gitleaks` over the changed files — passed.
- Every new guard was proven by planting the violation it forbids and confirming it fails: a second
  verifier call, a non-superseding concurrency group, an event trigger on the reusable publisher, a
  shallow checkout with its explanatory comment left in place, an ungated publisher (scope attack),
  a publisher that `needs:` the guard but ignores its conclusion, an unanchored suffix host match,
  a per-registry second build, a descriptor-count index assertion, a registry read inside the
  channel workflow, a registry credential in the package job, a removed review date, and a fixture
  pointed away from the protected branch. All thirteen failed as intended.

### Closed

`BL-E008-001` is marked `resolved`. Its residual half — `v*` tag pushes — is owned by
`E008-S01-002` / `E008-S01-003` and recorded in the issue's resolution note rather than left
implied.

### Review and fix round (fix iteration 1)

Findings: `../closure/review-E008-S01-001.md` — critical 0, high 2, medium 6, low 6. All HIGH and
MEDIUM findings were fixed; one LOW was deferred as `BL-E008-002`; the other five LOWs were cheap
enough to fix in the same pass.

- **HIGH-1 — the F16 re-check was enforced by nothing.** Both re-check steps could be deleted with
  the whole suite green. `test_step_based_publishers_re_read_the_tag_set_before_they_upload` now
  derives the set from `_publishers()`, restricted to jobs that have steps, and asserts a
  `stable_tags.py` invocation strictly before the first publishing step whose output is acted on.
  Proven twice: deleting a re-check, and leaving it in place while ignoring its result.
- **HIGH-2 — `git fetch` after `persist-credentials: false` is unauthenticated.** All four such
  fetches were removed. Freshness now comes from the authenticated `actions/checkout` immediately
  above each check, which already fetches full history and every tag. This also removed the
  `--reachable-from` call, whose remote-tracking ref could not be relied on without a fetch; the
  reachability primitive stays tested and available for story 003's finalizer, which CI-AR29
  requires to prove the candidate is still the protected default-branch head.
- **MEDIUM-1 — the descriptor-count prohibition matched one spelling in one file.** Replaced by
  `DESCRIPTOR_COUNT_ASSERTIONS`, three patterns covering `len(index["manifests"]) != 2`,
  `2 == len(manifests)` and `jq '.manifests | length'`, over every governed definition **plus**
  `scripts/*.py` — which is where F9 pushes non-trivial logic, and therefore where such an
  assertion would come to rest. Proven with all three spellings, including one relocated into
  `scripts/`.
- **MEDIUM-2 — the publisher derivation had two copies.** `_publishers()` is now the single
  definition; `test_any_push_triggered_workflow_verifies_before_it_ships` calls it. Re-proven by
  planting a publisher with no verifier dependency.
- **MEDIUM-3 — the fixture test was a checksum of the fixture.** It now extracts dev.yaml's
  `identity` step and executes it against the fixture event, asserting the emitted `image-tags`.
  Changing the `:0:12` slice or deleting the empty-version guard now fails it; both were planted.
- **MEDIUM-4 — "one Buildx invocation" was scoped to one hand-named file.** Extended to every
  channel workflow (derived from who calls the image publisher) and to the caller's `platforms:`
  input, so a `linux/amd64`-only call fails rather than shipping a single-platform image.
- **MEDIUM-5 — a tag could address a destination the run never logged into.** The compose step now
  rejects any tag whose authority is not the forge registry, or Docker Hub when Docker Hub is
  enabled, and rejects a reference with no `:tag`. The guard **runs** that step rather than reading
  it: a textual check passed with the rejection branch deleted and the variable left behind.
- **MEDIUM-6 / LOW-4 — prose overstated coverage.** The runbook now says the image publisher does
  not re-read inline and why; `.dev0` (unreachable, since the distance is at least 1) became
  `.devN`; the "fifth workflow file" claim became "fifth once release.yaml lands".
- **LOW-1** comment corrected to say what the third hash actually adds (redundancy) versus the
  identity match (reach). **LOW-2** both publishers and the image call now consume
  `needs.verify.outputs.dist-artifact-name`, and `image-reference` is consumed in the evidence
  table. **LOW-3** the review-date guard is anchored to the word `reviewed`, so deleting it while
  leaving the upstream commit date fails. **LOW-4** the positive halves of two guards read
  comment-stripped `run:` bodies, so a commented-out mechanism no longer satisfies "it is present"
  (prohibitions deliberately keep the raw body). **LOW-5** `_server_authority` keeps hostname and
  authority distinct, so an IPv6 forge brackets correctly and can be overridden, and `primary` uses
  `%:*` so a ported registry is not truncated. **LOW-6** TestPyPI is reported `unsupported` rather
  than `enabled` on a non-GitHub forge, since trusted publishing needs a GitHub OIDC identity —
  ADR-0011 §2's distinction, applied in the single producer.
- **Deferred:** `BL-E008-002` (Low) — `user: ${{ github.actor }}` pairs the pusher's username with
  the forge package token.

Final state: `poetry run pytest -q` — **202 passed**. `tests/ci/test_workflow_contracts.py` holds
47 guards (34 shipped, 13 added). **25 planted violations**, one per rule and one per scope,
all confirmed to fail the guard they attack.
