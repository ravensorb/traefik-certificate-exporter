---
status: done
---

# E002-S01-001: Declare the Missing Cryptography Dependency

- **Epic:** E002 — Reliable Installation, Build & Release Pipeline · **Sprint:** S01 · **Classification:** simple
- **State:** `_bmad-output/implementation-artifacts/state/planned/epic-002/sprint-01/E002-S01-001.yaml`
- **Source:** `_bmad-output/planning-artifacts/epics.md` (Epic 2, Story 2.1)

As a user installing via `pip install traefik-certificate-exporter`,
I want every import the package needs to be guaranteed by its declared dependencies,
So that the install doesn't fail or behave inconsistently depending on what else happens to
already be present on the system.

**Acceptance Criteria:**

**Given** `cryptography` (`x509`, `hazmat.primitives`, `pkcs12`) is imported in
`certificate_exporter.py` and `scripts/dump-pkcs12.py` but absent from
`[tool.poetry.dependencies]`
**When** `cryptography` is added with an appropriate version constraint and `poetry.lock` is
regenerated
**Then** a clean environment with only declared dependencies installed can import the package
with no `ImportError` (fulfills NFR3)

## Files in scope

- `pyproject.toml` — add `cryptography` to `[tool.poetry.dependencies]`
- `poetry.lock` — regenerate after the dependency change
- `src/traefik_certificate_exporter/libs/certificate_exporter.py`, `scripts/dump-pkcs12.py` — the two importers of `cryptography` that motivate this fix (no code changes expected, just verifying the import is now guaranteed)
